{
  "name": "fpl-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["engine", "server", "client"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "test": "npm run test --workspace=engine && npm run test --workspace=server",
    "build": "npm run build --workspace=engine && npm run build --workspace=server && npm run build --workspace=client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}
